import React from 'react'
import reduxGrid from 'redux-grid'

/** selector gets full redux state and sorts, filters, returns an iterable */
const selector = state => {


}
const { Grid } = reduxGrid({ selector })
export default props => <div id="grid-goes-here" />
